なぜRESTに従うのか

→ 業界で共通のルールに従うことで、設計・理解・保守を楽にするため

なぜ環境変数を使うのか

→ コードを変えずに、実行環境ごとの設定を切り替えるため

なぜHTTPメソッドを守るのか

→ リクエストの意味を標準化し、誤動作や事故を防ぐため

TCP = 信頼性を重視した通信プロトコル

「必ず」「順番通りに」「欠けずに」データを届ける
	•	ACK（確認応答）
	•	再送制御
